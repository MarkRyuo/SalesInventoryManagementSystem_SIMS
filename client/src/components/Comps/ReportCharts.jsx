


//* Reusable, Props 

const ReportCharts = ({className}) => {
    
    return (
        <>
            <div className={className}>
                {Reportobj.map((reports) => (
                    
                ))}
            </div>
        </>
    )
}

export default ReportCharts
