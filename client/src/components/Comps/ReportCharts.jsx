


//* Reusable, Props 

const ReportCharts = ({className, title, total}) => {
    
    return (
        <>
            <div className={className}>
                {Reportobj.map(() => (
                    
                ))}
            </div>
        </>
    )
}

export default ReportCharts
