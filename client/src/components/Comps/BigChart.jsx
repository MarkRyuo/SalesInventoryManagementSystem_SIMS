

//* Child 

const BigChart = ({bigreports}) => {


    return (
        <>
            {bigreports.map((bigreport) => (
                
            ))}
        </>
    );
}

export default BigChart;
